fn fib(n: Number) -> Number {
  let a: Number = 0;
  let b: Number = 1;
  let i: Number = 0;
  while (i < n) {
    let tmp: Number = a;
    a = b;
    b = tmp + b;
    i = i + 1;
  }

  return a;
}

print fib(40);